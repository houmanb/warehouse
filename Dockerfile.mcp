FROM python:3.11-slim

WORKDIR /app
ENV PYTHONUNBUFFERED=1

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY mcp_server.py .

# MCP servers speak JSON-RPC over stdio; do not print to stdout.
CMD ["python", "mcp_server.py"]
